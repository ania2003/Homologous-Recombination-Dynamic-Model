# Data

`homologous_recombination_interactions.tsv` contains the starting HR interaction network.

The file is tab-separated and has three columns: source node, interaction type, and target node. The first and third columns share the header `NODE`, so the analysis scripts rename them internally to `source` and `target`.
